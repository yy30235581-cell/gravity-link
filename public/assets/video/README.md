# Cutscene Videos

Place playable cutscene videos in this folder.

Use them in the frontend with paths like:

`/assets/video/intro.mp4`

Recommended formats: `.mp4` for broad browser support, with optional `.webm` alternatives.

